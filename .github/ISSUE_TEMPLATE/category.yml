name: Category Module
description: Category management system
title: "[CATEGORY] "
labels: ["category"]

body:
  - type: markdown
    attributes:
      value: |
        Select one or more tasks.

  - type: checkboxes
    id: core
    attributes:
      label: Core API
      options:
        - label: Create Category schema and model
        - label: GET all categories
        - label: GET single category
        - label: POST create category (admin only)
        - label: PUT/PATCH update category (admin only)
        - label: DELETE category (admin only)

  - type: checkboxes
    id: relations
    attributes:
      label: Relationships
      options:
        - label: Link products to categories