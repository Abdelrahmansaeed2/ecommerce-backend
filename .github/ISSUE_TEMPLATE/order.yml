name: Order Module
description: Order processing system
title: "[ORDER] "
labels: ["order"]

body:
  - type: markdown
    attributes:
      value: |
        Select one or more tasks.

  - type: checkboxes
    id: core
    attributes:
      label: Core API
      options:
        - label: Create Order schema and model
        - label: Create order endpoint
        - label: GET all orders (admin only)
        - label: GET user orders
        - label: Calculate total price

  - type: checkboxes
    id: relations
    attributes:
      label: Relationships
      options:
        - label: Link order to user
        - label: Link order to products